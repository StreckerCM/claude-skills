# Normalize all text files to LF in the repository and on checkout.
* text=auto eol=lf

# PowerShell scripts stay CRLF on checkout. Windows PowerShell 5.1 mishandles
# LF-only here-strings.
*.ps1 text eol=crlf

# Shell scripts must stay LF, even if the default above changes.
*.sh text eol=lf
